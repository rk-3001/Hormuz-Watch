I need you to replace the hardcoded scenario lookup table in my backend with a real parametric calculation. Here's the exact change:
File: artifacts/api-server/src/mockData.ts

Find the scenarioResultMap object and the computeScenarioResult function. Do NOT delete the three existing scenario entries (hormuz-closure, opec-emergency-cut, redsea-suspension) — keep their data as reference/calibration targets in a comment above the new function.
Replace computeScenarioResult with a new function that takes structured inputs and derives outputs mathematically instead of looking them up:

typescriptexport interface ScenarioInputs {
  disruptionPercent: number;   // 0-100, how much of the corridor's capacity is disrupted
  affectedCorridor: "hormuz" | "redsea" | "persian_gulf";
  durationDays: number;        // assumed duration of the disruption
}

export interface ScenarioResultBase {
  scenarioId: string;
  scenarioName: string;
  refineryRunRateDrop: number;
  fuelPriceDeltaPercent: number;
  sprDaysRemaining: number;
  gdpDragPercent: number;
  narrative: string;
  generatedAt: string;
}

// Corridor import share of India's total crude imports (approximate, per problem brief: 40-45% via Hormuz)
const CORRIDOR_IMPORT_SHARE: Record<string, number> = {
  hormuz: 0.42,
  redsea: 0.18,
  persian_gulf: 0.30,
};

// Calibration constants — back-solved so the formula reproduces the original
// hand-authored hormuz-closure scenario (50% disruption, corridor=hormuz) as closely as possible.
// Adjust these three constants until computeScenarioResult({disruptionPercent:50, affectedCorridor:"hormuz", durationDays:14})
// produces numbers within ~10% of: runRateDrop=34.2, priceDelta=28.6, gdpDrag=1.4
const RUN_RATE_COEFFICIENT = 163;   // volumeAtRisk * this = refineryRunRateDrop
const PRICE_DELTA_COEFFICIENT = 136;
const GDP_DRAG_COEFFICIENT = 6.7;
const SPR_DRAWDOWN_RATE = 0.4;      // days of SPR consumed per day of disruption, scaled by volumeAtRisk
const BASELINE_SPR_DAYS = 9.5;

export function computeScenarioResult(inputs: ScenarioInputs): ScenarioResultBase {
  const corridorShare = CORRIDOR_IMPORT_SHARE[inputs.affectedCorridor] ?? 0.25;
  const volumeAtRisk = corridorShare * (inputs.disruptionPercent / 100);

  const refineryRunRateDrop = round1(volumeAtRisk * RUN_RATE_COEFFICIENT);
  const fuelPriceDeltaPercent = round1(volumeAtRisk * PRICE_DELTA_COEFFICIENT);
  const sprDaysRemaining = round1(Math.max(0, BASELINE_SPR_DAYS - inputs.durationDays * volumeAtRisk * SPR_DRAWDOWN_RATE));
  const gdpDragPercent = round1(volumeAtRisk * GDP_DRAG_COEFFICIENT);

  const scenarioName = `${inputs.affectedCorridor} disruption (${inputs.disruptionPercent}%, ${inputs.durationDays}d)`;
  const scenarioId = `custom-${inputs.affectedCorridor}-${inputs.disruptionPercent}-${inputs.durationDays}`;

  const narrative = `A ${inputs.disruptionPercent}% capacity disruption in the ${inputs.affectedCorridor} corridor over ${inputs.durationDays} days would strip approximately ${(volumeAtRisk * 100).toFixed(1)}% of India's total crude import volume. Refineries projected to operate at ${(100 - refineryRunRateDrop).toFixed(1)}% run-rate. Fuel prices projected +${fuelPriceDeltaPercent}%. SPR cover falls to ${sprDaysRemaining} days at modeled drawdown. GDP drag estimated at ${gdpDragPercent}% annualised.`;

  return { scenarioId, scenarioName, refineryRunRateDrop, fuelPriceDeltaPercent, sprDaysRemaining, gdpDragPercent, narrative, generatedAt: new Date().toISOString() };
}

function round1(n: number): number {
  return Math.round(n * 10) / 10;
}

After writing this, add a temporary test script (or a console.log at the bottom of mockData.ts behind an if (require.main === module) check) that calls computeScenarioResult with the three original scenario parameters (Hormuz 50%/14d, OPEC+ cut — treat as persian_gulf 100%/10d, Red Sea 100%/16d) and prints the output next to the original hardcoded values, so I can visually confirm the calibration constants are close. Iterate on RUN_RATE_COEFFICIENT, PRICE_DELTA_COEFFICIENT, GDP_DRAG_COEFFICIENT, and SPR_DRAWDOWN_RATE until the outputs are within ~10-15% of the original three scenarios.

File: artifacts/api-server/src/routes/scenarios.ts

Update the POST /scenarios/:id/run route to still support the 3 preset scenario IDs (map each preset ID to its {disruptionPercent, affectedCorridor, durationDays} inputs and call the new computeScenarioResult), AND add a new route:

typescriptrouter.post("/scenarios/custom/run", (req, res) => {
  const { disruptionPercent, affectedCorridor, durationDays } = req.body;
  if (typeof disruptionPercent !== "number" || typeof durationDays !== "number" ||
      !["hormuz", "redsea", "persian_gulf"].includes(affectedCorridor)) {
    res.status(400).json({ error: "Invalid input: expected disruptionPercent (number), affectedCorridor (hormuz|redsea|persian_gulf), durationDays (number)" });
    return;
  }
  const result = computeScenarioResult({ disruptionPercent, affectedCorridor, durationDays });
  res.json(result);
});

Do not touch routes/recommendations.ts or procurementOptions yet — I'll give you a separate prompt for that once I've confirmed the ranking logic there.

After making these changes, show me the console output comparing the new calculated values against the original three hardcoded scenarios so I can confirm calibration accuracy.