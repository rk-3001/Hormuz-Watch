The Command Center's scenario input box is not behaving as specified. I typed a scenario description and got back a full conversational paragraph instead of a structured interpretation with pre-filled sliders. This means the frontend is likely calling the wrong endpoint, or the interpret endpoint itself isn't returning structured JSON. Diagnose and fix:

Open the Command Center component and show me exactly which endpoint the scenario text box's submit handler calls — paste the fetch/API call code. Confirm whether it's calling /api/scenarios/interpret (correct) or an existing conversational/chat endpoint under routes/anthropic/ (incorrect).
Independently, test POST /api/scenarios/interpret directly (curl or server console) with body {"text": "A full blockade of the Strait of Hormuz for a month..."} and paste the raw response. Confirm it returns valid JSON matching {disruptionPercent, affectedCorridor, durationDays, confidence, interpretation} — not prose.
If the backend route is correct but the frontend is calling the wrong endpoint: fix the Command Center submit handler to call /api/scenarios/interpret specifically, and confirm the response is parsed into the confidence badge + pre-filled sliders as originally specified, not rendered as a chat message.
If the backend route itself is returning prose instead of JSON: the Groq system prompt is likely not being enforced strictly enough. Add explicit constraints to the system prompt: "Respond with ONLY a single JSON object. No explanation, no markdown, no additional text before or after the JSON." Also set response_format: { type: "json_object" } if the Groq API supports it for this model (check Groq's docs for llama-3.3-70b-versatile JSON mode support) as a hard guarantee rather than relying on prompt instructions alone.
After fixing, re-run the exact test: submit "A full blockade of the Strait of Hormuz for a month..." through the Command Center UI and paste back the actual rendered interpretation banner, confidence badge, and pre-filled slider values — not a chat response.